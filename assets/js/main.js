const root=document.documentElement;
const themeButton=document.querySelector('.theme-toggle');themeButton?.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=next;try{localStorage.setItem('theme',next)}catch(e){}});
const navToggle=document.querySelector('.nav-toggle'),nav=document.querySelector('.site-nav');navToggle?.addEventListener('click',()=>{const open=nav?.classList.toggle('open')||false;navToggle.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');navToggle?.setAttribute('aria-expanded','false')}));
document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
const reveals=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});reveals.forEach(el=>observer.observe(el))}else reveals.forEach(el=>el.classList.add('visible'));
document.querySelectorAll('pre').forEach(pre=>{const btn=document.createElement('button');btn.className='code-copy';btn.type='button';btn.textContent='Copy';btn.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(pre.innerText);btn.textContent='Copied';setTimeout(()=>btn.textContent='Copy',1400)}catch(e){}});pre.style.position='relative';btn.style.cssText='position:absolute;right:10px;top:10px;border:1px solid #445;background:#1b272b;color:#fff;border-radius:8px;padding:7px 10px;min-height:40px;cursor:pointer';pre.appendChild(btn)});
/* Compatibility only for older static pages not yet regenerated. */
document.querySelectorAll('a[href="/learning-r/"]').forEach(link=>{link.href='/teaching/';if(link.textContent.trim()==='Learning R')link.textContent='Teaching'});document.querySelectorAll('a[href="/about/"]').forEach(link=>{if(link.textContent.trim()==='Background')link.textContent='Profile'});document.querySelectorAll('.site-nav a[href="/cv/"],.footer-links a[href="/cv/"]').forEach(link=>link.remove());
